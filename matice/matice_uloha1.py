class Matica:
    def __init__(self, r, s, prvky = None):
        self.r = r
        self.s = s


        if prvky is not None:
            if len(prvky) == self.r * self.s:
                self.
            else:
                raise ValueError("Nesprávny počet prvkov pre maticu.")
            